# ===== AI 旅行搭子 前端配置 =====
# 复制为 .env 后填写（Vite 只会把 VITE_ 前缀的变量暴露给前端）

# 高德 JS API Key（注意：这是「Web端(JS API)」类型的 Key，
# 不是后端 seed 用的「Web服务」Key，两个不通用）
# 不填也能跑：TripMap.vue 会用 GCJ-02 经纬度画 SVG 示意图
VITE_AMAP_JS_KEY=

# 高德 JS API 2.0 需要的安全密钥（在控制台给 Key 绑定安全密钥时生成）
VITE_AMAP_SECURITY_CODE=
